{
  "imports": ["import { envField } from 'astro/config';"],
  "config": {
    "env": {
      "schema": {
        "CONVEX_URL": {
          "$expr": "envField.string({ access: 'public', context: 'client' })"
        }
      }
    }
  }
}
